{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
{#    <div class="body-wrapper">#}
{#        <div class="card">#}
{#            <div class="card-body">#}
{#                <p>{{ category.name }}</p>#}
{#                <p>{{ category.feedLastUpdatedAt ? category.feedLastUpdatedAt|date('Y-m-d H:i:s') : '' }}</p>#}
{#            </div>#}
{#        </div>#}

{#    </div>#}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td></td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
            <tr>
                <th>Feed_url</th>
                <td>{{ category.feedUrl }}</td>
            </tr>
            <tr>
                <th>Feed_last_updated_at</th>
                <td>{{ category.feedLastUpdatedAt ? category.feedLastUpdatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_index') }}">back to list</a>

    <a href="{{ path('app_category_edit', {'id': category.id}) }}">edit</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
